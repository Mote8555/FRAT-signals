[build]
  command = "cd client && npm ci && npm run build"
  publish = "client/dist"

[[redirects]]
  from = "/api/*"
  to = "https://frat-signals.onrender.com/api/:splat"
  status = 200
  force = false
